# Research Team Feature - UI Flow

## Screen Flow

```
┌─────────────────────────────────────┐
│   ResearchTeamListFragment          │
│  ┌─────────────────────────────┐   │
│  │  🔬 Лаборатория AI          │   │
│  │  👤 Лидер: Иванов И.И.      │   │
│  │  👥 5 участников  📝 3 работы│   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │  🔬 НИИ Квантовых систем    │   │
│  │  👤 Лидер: Петров П.П.      │   │
│  │  👥 3 участника  📝 2 работы │   │
│  └─────────────────────────────┘   │
│                                     │
│              [+] FAB                │
└─────────────────────────────────────┘
              │ (tap on item)
              ▼
┌─────────────────────────────────────┐
│  ResearchTeamDetailFragment         │
│  ┌───────────────────────────────┐ │
│  │        🔬                     │ │
│  │   Лаборатория AI              │ │
│  │   👤 Лидер: Иванов И.И.       │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ Описание: Исследование AI...  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌──────────┬──────────────────┐   │
│  │Участники │ Научные работы   │   │ ← TabLayout
│  └──────────┴──────────────────┘   │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Иванов Иван                   │ │
│  │ Сотрудник           MEMBER    │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ Сидоров Петр                  │ │
│  │ Студент             MEMBER    │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

## Component Architecture

```
┌──────────────────────────────────────────────────────┐
│                    UI Layer                          │
│  ┌──────────────────┐      ┌────────────────────┐  │
│  │ ListFragment     │      │ DetailFragment     │  │
│  │ - RecyclerView   │      │ - TabLayout        │  │
│  │ - FAB            │      │ - 2 RecyclerViews  │  │
│  └──────────────────┘      └────────────────────┘  │
│           │                          │               │
│           └──────────┬───────────────┘               │
└──────────────────────┼───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│                ViewModel Layer                       │
│  ┌─────────────────────────────────────────────┐   │
│  │     ResearchTeamViewModel                    │   │
│  │  - teams: LiveData<Resource<List<Team>>>    │   │
│  │  - teamDetail: LiveData<Resource<Team>>     │   │
│  │  - loadTeams()                               │   │
│  │  - loadTeamDetail(id)                        │   │
│  │  - createTeam(...)                           │   │
│  └─────────────────────────────────────────────┘   │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│              Repository Layer                        │
│  ┌─────────────────────────────────────────────┐   │
│  │     ResearchTeamRepository                   │   │
│  │  - getResearchTeams()                        │   │
│  │  - getResearchTeamById(id)                   │   │
│  │  - createTeam(request)                       │   │
│  │  - addEmployeeToTeam(request)                │   │
│  └─────────────────────────────────────────────┘   │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│                 API Layer                            │
│  ┌─────────────────────────────────────────────┐   │
│  │           ApiService (Retrofit)              │   │
│  │  - GET /api/research-teams                   │   │
│  │  - GET /api/research-teams/{id}              │   │
│  │  - POST /api/research-teams                  │   │
│  │  - POST /api/research-teams/members/...      │   │
│  │  - POST /api/research-teams/works            │   │
│  └─────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

## Data Models

```kotlin
ResearchTeam
├── id: Long
├── name: String
├── description: String?
├── leader: Employee
├── createdAt: String?
├── members: List<TeamMember>?
└── researchWorks: List<TeamResearchWork>?

TeamMember
├── id: Long
├── employee: Employee?
├── student: Student?
├── role: String
├── joinedAt: String?
└── Helper methods: getMemberName(), getMemberType()

TeamResearchWork
├── id: Long
├── title: String
├── description: String
├── status: String (IN_PROGRESS, COMPLETED, PUBLISHED, ON_HOLD)
├── startDate: String?
├── endDate: String?
├── createdAt: String?
└── Helper methods: getStatusText(), getStatusColor()
```

## Features Implemented

✅ Team List with search and filtering
✅ Team Details with tabs for members and works
✅ Material Design cards with gradient backgrounds
✅ Status badges with colors for research works
✅ Empty states and loading indicators
✅ Navigation with Safe Args
✅ Error handling with Resource wrapper
✅ Proper lifecycle management with ViewBinding

## API Integration

All endpoints follow RESTful conventions:
- GET: Retrieve data
- POST: Create new resources
- PUT: Update resources
- DELETE: Remove resources

Authentication handled by AuthInterceptor with JWT tokens.
